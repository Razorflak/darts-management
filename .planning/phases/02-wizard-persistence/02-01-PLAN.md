---
phase: 02-wizard-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/008_phase_table.sql
  - packages/front/src/lib/tournament/types.ts
autonomous: true
requirements: [EVENT-01, EVENT-02, EVENT-03, EVENT-04, EVENT-05, EVENT-06]

must_haves:
  truths:
    - "La migration 008 existe et crée la table `phase` avec les colonnes exactes du schéma CONTEXT.md"
    - "La colonne `phases JSONB` de la table `tournament` est supprimée par la migration"
    - "Le type `PublishOptions` est supprimé de types.ts"
    - "PublishOptions est supprimée de types.ts. Le typecheck global sera vérifié après la complétion de Wave 2 (plan 02-05)."
  artifacts:
    - path: "packages/db/src/schema/008_phase_table.sql"
      provides: "Migration SQL créant la table phase et supprimant phases JSONB de tournament"
      contains: "CREATE TABLE phase"
    - path: "packages/front/src/lib/tournament/types.ts"
      provides: "Types TypeScript du wizard sans PublishOptions"
      contains: "EliminationPhase"
  key_links:
    - from: "packages/db/src/schema/008_phase_table.sql"
      to: "packages/db/src/schema/007_tournament.sql"
      via: "ALTER TABLE tournament DROP COLUMN phases"
      pattern: "DROP COLUMN phases"
    - from: "packages/front/src/lib/tournament/types.ts"
      to: "packages/front/src/lib/tournament/components/PublishStep.svelte"
      via: "PublishOptions supprimé — PublishStep.svelte devra être mis à jour séparément (plan 02-05)"
      pattern: "PublishOptions"
---

<objective>
Créer la migration SQL 008 qui remplace la colonne `phases JSONB` de la table `tournament` par une table normalisée `phase`, et supprimer le type `PublishOptions` de types.ts (devenu inutile après suppression des checkboxes de PublishStep).

Purpose: Foundation bloquante — tous les plans de Wave 2 dépendent de la table `phase` existante. Sans cette migration, les endpoints save/publish/edit ne peuvent pas être mis à jour.
Output: Fichier SQL 008 + types.ts épuré.
</objective>

<execution_context>
@/home/jta/.claude/get-shit-done/workflows/execute-plan.md
@/home/jta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-wizard-persistence/02-CONTEXT.md
@.planning/phases/02-wizard-persistence/02-RESEARCH.md
@packages/db/src/schema/007_tournament.sql
@packages/front/src/lib/tournament/types.ts
</context>

<interfaces>
<!-- Schéma exact de la table phase (depuis CONTEXT.md — NON NÉGOCIABLE) -->
```sql
CREATE TABLE phase (
  id                   UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tournament_id        UUID NOT NULL REFERENCES tournament(id) ON DELETE CASCADE,
  position             INTEGER NOT NULL,      -- ordre dans le tournoi
  type                 TEXT NOT NULL,         -- round_robin | double_loss_groups | single_elim | double_elim
  entrants             INTEGER NOT NULL,      -- tous types
  -- Poules (round_robin + double_loss_groups)
  players_per_group    INTEGER,               -- joueurs par poule
  qualifiers_per_group INTEGER,               -- qualifiés par poule
  -- Brackets (single_elim + double_elim)
  qualifiers           INTEGER,               -- qualifiés vers phase suivante (NULL = phase finale)
  tiers                JSONB,                 -- [{id, round, legs}]
  created_at           TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

<!-- Mapping TypeScript Phase → colonnes SQL -->
GroupPhase (round_robin | double_loss_groups):
  id → ignoré (UUID généré par DB)
  type → type TEXT
  entrants → entrants INTEGER
  qualifiers → qualifiers_per_group INTEGER   (NB: champ GroupPhase.qualifiers = qualifiers_per_group en SQL)
  playersPerGroup → players_per_group INTEGER
  players_per_group_ts: group phase n'a PAS de colonne `qualifiers` SQL — c'est qualifiers_per_group

EliminationPhase (single_elim | double_elim):
  id → ignoré
  type → type TEXT
  entrants → entrants INTEGER
  qualifiers → qualifiers INTEGER (nullable)
  tiers → tiers JSONB (JSON.stringify(phase.tiers))
  players_per_group → NULL
  qualifiers_per_group → NULL
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Créer la migration 008_phase_table.sql</name>
  <files>packages/db/src/schema/008_phase_table.sql</files>
  <action>
Créer le fichier `packages/db/src/schema/008_phase_table.sql` avec exactement ce contenu :

1. Créer la table `phase` avec le schéma exact défini dans CONTEXT.md (voir bloc `<interfaces>` ci-dessus).
2. Créer un index sur `tournament_id` pour les requêtes JOIN/WHERE.
3. Supprimer la colonne `phases` de la table `tournament` avec ALTER TABLE.

Contenu attendu du fichier :

```sql
-- Migration 008: Replace phases JSONB column with dedicated phase table
-- Per CONTEXT.md decision: phases stored in normalized phase table, not JSONB

CREATE TABLE phase (
  id                   UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tournament_id        UUID NOT NULL REFERENCES tournament(id) ON DELETE CASCADE,
  position             INTEGER NOT NULL,
  type                 TEXT NOT NULL,
  entrants             INTEGER NOT NULL,
  players_per_group    INTEGER,
  qualifiers_per_group INTEGER,
  qualifiers           INTEGER,
  tiers                JSONB,
  created_at           TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX phase_tournament_idx ON phase(tournament_id);

-- Remove legacy JSONB column from tournament
ALTER TABLE tournament DROP COLUMN phases;
```

Ne pas appliquer la migration — c'est aux endpoints d'y procéder via le script de migration habituel.
  </action>
  <verify>
    <automated>cat /home/jta/Projects/darts-management/master/packages/db/src/schema/008_phase_table.sql | grep -E "CREATE TABLE phase|DROP COLUMN phases|phase_tournament_idx"</automated>
  </verify>
  <done>Le fichier 008_phase_table.sql existe, contient CREATE TABLE phase avec toutes les colonnes du schéma, l'index phase_tournament_idx, et ALTER TABLE tournament DROP COLUMN phases.</done>
</task>

<task type="auto">
  <name>Task 2: Supprimer PublishOptions de types.ts</name>
  <files>packages/front/src/lib/tournament/types.ts</files>
  <action>
Supprimer l'interface `PublishOptions` de `packages/front/src/lib/tournament/types.ts`.

L'interface à supprimer (lignes 77-80 actuellement) :
```typescript
export interface PublishOptions {
  notifications: boolean
  openRegistrations: boolean
}
```

Cette interface sera également supprimée des imports dans les pages wizard (`events/new/+page.svelte`, `events/[id]/edit/+page.svelte`) et dans `PublishStep.svelte` — ces fichiers seront mis à jour dans le plan 02-05. Pour l'instant, supprimer uniquement la déclaration dans types.ts.

Note : `PublishOptions` est encore importée par PublishStep.svelte et les pages wizard — typecheck ÉCHOUERA après cette tâche tant que le plan 02-05 n'a pas été exécuté. C'est attendu. Le typecheck final sera vérifié une fois tous les plans de Wave 2 terminés.
  </action>
  <verify>
    <automated>grep -n "PublishOptions" /home/jta/Projects/darts-management/master/packages/front/src/lib/tournament/types.ts | wc -l</automated>
  </verify>
  <done>grep retourne 0 — PublishOptions n'apparaît plus dans types.ts.</done>
</task>

</tasks>

<verification>
```bash
# Migration SQL existe avec le bon contenu
ls /home/jta/Projects/darts-management/master/packages/db/src/schema/008_phase_table.sql
grep "CREATE TABLE phase" /home/jta/Projects/darts-management/master/packages/db/src/schema/008_phase_table.sql
grep "DROP COLUMN phases" /home/jta/Projects/darts-management/master/packages/db/src/schema/008_phase_table.sql

# PublishOptions supprimé de types.ts
grep "PublishOptions" /home/jta/Projects/darts-management/master/packages/front/src/lib/tournament/types.ts
# doit retourner vide
```
</verification>

<success_criteria>
- 008_phase_table.sql existe avec CREATE TABLE phase (9 colonnes + created_at), index phase_tournament_idx, ALTER TABLE tournament DROP COLUMN phases
- types.ts ne contient plus la déclaration interface PublishOptions
- Les colonnes SQL correspondent exactement au schéma CONTEXT.md
</success_criteria>

<output>
After completion, create `.planning/phases/02-wizard-persistence/02-01-SUMMARY.md`
</output>
