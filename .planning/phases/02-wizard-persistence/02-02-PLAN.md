---
phase: 02-wizard-persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/front/src/lib/tournament/templates.ts
  - packages/front/src/lib/tournament/labels.ts
  - packages/front/src/lib/tournament/components/phases/AddPhaseMenu.svelte
  - packages/front/src/lib/tournament/components/phases/BracketTiers.svelte
  - packages/db/src/schema/003_seed_dev.sql
  - packages/front/vite.config.ts
  - packages/front/src/routes/(app)/events/[id]/edit/+page.server.ts
autonomous: true
requirements: [EVENT-01, EVENT-02, EVENT-03, EVENT-04, EVENT-05, EVENT-06]
gap_closure: true

must_haves:
  truths:
    - "Le build part d'un checkout propre sans erreur — templates.ts est tracké par git"
    - "Les catégories double, double_female, double_mix s'affichent correctement (pas 'undefined') dans l'UI"
    - "Les dropdowns 'Ajouter une phase' et 'Ajouter un palier' s'ouvrent et se ferment correctement"
    - "La requête SQL du load /events/[id]/edit ne référence pas la colonne inexistante start_time sur event"
    - "Le re-seed de la DB ne produit pas d'erreur de contrainte FK"
  artifacts:
    - path: "packages/front/src/lib/tournament/templates.ts"
      provides: "EventTemplate, TournamentTemplate, PhaseTemplate types et EVENT_TEMPLATES array"
      exports: ["EventTemplate", "TournamentTemplate", "PhaseTemplate", "EVENT_TEMPLATES"]
    - path: "packages/front/src/lib/tournament/labels.ts"
      provides: "CATEGORY_LABELS couvrant toutes les valeurs Category incluant double/double_female/double_mix"
      contains: "double_mix"
    - path: "packages/front/src/routes/(app)/events/[id]/edit/+page.server.ts"
      provides: "Load function sans référence à event.start_time"
      contains: "registration_opens_at::text"
  key_links:
    - from: "packages/front/src/lib/tournament/components/TemplateModal.svelte"
      to: "packages/front/src/lib/tournament/templates.ts"
      via: "import { EVENT_TEMPLATES } from '../templates.js'"
      pattern: "EVENT_TEMPLATES"
---

<objective>
Committer tous les fichiers non commités du working tree accumulés lors de la phase 2 gap-closure. Ces 7 fichiers corrigent des bugs réels trouvés pendant ou après l'UAT, notamment templates.ts qui est déjà importé par du code commité et empêche un checkout propre de builder.

Purpose: Assainir le working tree avant les plans de Wave 2. Sans ce plan, les 7 fichiers modifiés/non-trackés parasiteront les commits des plans suivants.
Output: Working tree propre pour les 7 fichiers. 2 commits atomiques (frontend + DB seed). Build reproductible depuis checkout frais.
</objective>

<execution_context>
@/home/jta/.claude/get-shit-done/workflows/execute-plan.md
@/home/jta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-wizard-persistence/02-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Committer les corrections frontend (templates.ts, labels.ts, Svelte components, vite.config.ts, edit server)</name>
  <files>
    packages/front/src/lib/tournament/templates.ts,
    packages/front/src/lib/tournament/labels.ts,
    packages/front/src/lib/tournament/components/phases/AddPhaseMenu.svelte,
    packages/front/src/lib/tournament/components/phases/BracketTiers.svelte,
    packages/front/vite.config.ts,
    packages/front/src/routes/(app)/events/[id]/edit/+page.server.ts
  </files>
  <action>
Stager les 6 fichiers frontend listés et créer un commit atomique.

Ce que chaque fichier corrige :
- `packages/front/src/lib/tournament/templates.ts` — NOUVEAU FICHIER (non-tracké). Définit EventTemplate, TournamentTemplate, PhaseTemplate et EVENT_TEMPLATES. Déjà importé par TemplateModal.svelte — sans ce commit tout checkout propre échoue à builder.
- `packages/front/src/lib/tournament/labels.ts` — MODIFIÉ. Ajout des 3 entrées manquantes dans CATEGORY_LABELS : `double: 'Double'`, `double_female: 'Double Féminin'`, `double_mix: 'Double Mixte'`. Le type Category incluait ces valeurs mais les labels manquants causaient l'affichage 'undefined'.
- `packages/front/src/lib/tournament/components/phases/AddPhaseMenu.svelte` — MODIFIÉ. Remplacement du Dropdown Flowbite (ne rendait pas correctement en Svelte 5) par un bouton natif HTML + backdrop fixe (z-10) + menu absolu (z-20).
- `packages/front/src/lib/tournament/components/phases/BracketTiers.svelte` — MODIFIÉ. Même remplacement Flowbite → natif pour le dropdown "Ajouter un palier".
- `packages/front/vite.config.ts` — MODIFIÉ. Normalisation whitespace uniquement (tabs → spaces). Aucun changement fonctionnel.
- `packages/front/src/routes/(app)/events/[id]/edit/+page.server.ts` — MODIFIÉ. Supprime la référence à `start_time` dans la requête SQL sur la table `event` (start_time est une colonne de `tournament`, pas de `event`). Fix d'une erreur SQL potentielle.

Commandes à exécuter :
```bash
cd /home/jta/Projects/darts-management/master
git add packages/front/src/lib/tournament/templates.ts \
        packages/front/src/lib/tournament/labels.ts \
        packages/front/src/lib/tournament/components/phases/AddPhaseMenu.svelte \
        packages/front/src/lib/tournament/components/phases/BracketTiers.svelte \
        packages/front/vite.config.ts \
        packages/front/src/routes/(app)/events/\[id\]/edit/+page.server.ts
git commit -m "fix(02-wizard): post-UAT frontend fixes — templates.ts, label gaps, native dropdowns, edit server"
```

Ne pas utiliser `git add -A` ou `git add .` — stager uniquement les 6 fichiers listés.
  </action>
  <verify>
    <automated>cd /home/jta/Projects/darts-management/master && git show --stat HEAD | grep -E "templates\.ts|labels\.ts|AddPhaseMenu|BracketTiers|vite\.config|edit/\+page\.server" | wc -l</automated>
  </verify>
  <done>Le commit existe contenant les 6 fichiers frontend. `git show --stat HEAD` liste tous les 6 chemins. `git status` ne les montre plus comme modifiés/non-trackés.</done>
</task>

<task type="auto">
  <name>Task 2: Committer la correction du seed DB (003_seed_dev.sql)</name>
  <files>packages/db/src/schema/003_seed_dev.sql</files>
  <action>
Stager le fichier seed et le commiter séparément des corrections frontend (concerné différent : backend/data).

Ce que ce fichier corrige :
- TRUNCATE inclut maintenant `user_entity_role` en premier (manquant → erreur de contrainte FK au re-seed car les lignes user_entity_role référencent user et entity)
- Ajout du user de test `tanguyj35@gmail.com` avec rôle `adminFederal` sur la fédération (compte développeur réel pour les tests manuels)

Commandes :
```bash
cd /home/jta/Projects/darts-management/master
git add packages/db/src/schema/003_seed_dev.sql
git commit -m "fix(02-wizard): seed — add user_entity_role to TRUNCATE, add tanguy test user"
```
  </action>
  <verify>
    <automated>cd /home/jta/Projects/darts-management/master && git show --stat HEAD | grep "003_seed_dev.sql" | wc -l</automated>
  </verify>
  <done>Commit avec 003_seed_dev.sql en tête. `git status` ne montre plus ce fichier. La ligne TRUNCATE dans le fichier commité inclut `user_entity_role`.</done>
</task>

</tasks>

<verification>
```bash
# Tous les fichiers sont commités (plus dans le working tree diff)
cd /home/jta/Projects/darts-management/master
git status --short | grep -E "templates\.ts|labels\.ts|AddPhaseMenu|BracketTiers|vite\.config|edit.*server|003_seed"

# templates.ts est tracké
git ls-files packages/front/src/lib/tournament/templates.ts

# CATEGORY_LABELS contient double_mix
git show HEAD~1:packages/front/src/lib/tournament/labels.ts | grep "double_mix"

# Seed TRUNCATE inclut user_entity_role
git show HEAD:packages/db/src/schema/003_seed_dev.sql | grep "user_entity_role"
```
</verification>

<success_criteria>
- templates.ts est tracké par git — `git ls-files packages/front/src/lib/tournament/templates.ts` retourne le chemin
- labels.ts commité inclut les entrées double, double_female, double_mix
- AddPhaseMenu.svelte et BracketTiers.svelte utilisent des dropdowns natifs (sans import Flowbite Dropdown)
- 003_seed_dev.sql TRUNCATE inclut user_entity_role
- edit/+page.server.ts ne référence plus start_time dans la requête sur event
- vite.config.ts whitespace normalisé commité
- `git status --short` ne liste aucun des 7 fichiers
</success_criteria>

<output>
After completion, create `.planning/phases/02-wizard-persistence/02-02-SUMMARY.md`
</output>
