---
phase: 02-wizard-persistence
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-04]
files_modified:
  - packages/front/src/lib/tournament/components/PublishStep.svelte
  - packages/front/src/routes/(app)/events/+page.svelte
autonomous: true
requirements: [EVENT-05, EVENT-06]

must_haves:
  truths:
    - "PublishStep n'affiche plus les 2 checkboxes (Notifier les membres, Ouvrir les inscriptions)"
    - "PublishStep accepte un prop eventStatus et conditionne le bouton Publier sur statut draft"
    - "Si statut ready ou started, PublishStep affiche uniquement le résumé et le bouton Enregistrer"
    - "La liste /events affiche un lien d'édition pour les événements ready et started (pas uniquement draft)"
    - "PublishStep n'importe plus PublishOptions"
  artifacts:
    - path: "packages/front/src/lib/tournament/components/PublishStep.svelte"
      provides: "PublishStep sans checkboxes, avec bouton Publier conditionnel au statut draft"
      contains: "eventStatus"
    - path: "packages/front/src/routes/(app)/events/+page.svelte"
      provides: "Liste d'événements avec lien d'édition pour draft, ready, started"
      contains: "ready"
  key_links:
    - from: "packages/front/src/routes/(app)/events/[id]/edit/+page.svelte"
      to: "packages/front/src/lib/tournament/components/PublishStep.svelte"
      via: "eventStatus={data.eventStatus}"
      pattern: "eventStatus"
    - from: "packages/front/src/routes/(app)/events/+page.svelte"
      to: "packages/front/src/routes/(app)/events/[id]/edit/+page.server.ts"
      via: "href='/events/{event.id}/edit' conditionné sur status !== 'finished'"
      pattern: "finished"
---

<objective>
Nettoyer PublishStep en supprimant les 2 checkboxes d'options et en conditionnant le bouton Publier sur le statut draft. Mettre à jour la liste /events pour afficher des liens d'édition pour les événements ready et started (pas uniquement draft).

Purpose: Deux décisions CONTEXT.md non encore implémentées — suppression des checkboxes PublishStep et édition des événements publiés depuis la liste. Plan 02-05 est parallèle à 02-03 et 02-04.
Output: PublishStep épuré + liste /events avec liens edit pour tous statuts éditables.
</objective>

<execution_context>
@/home/jta/.claude/get-shit-done/workflows/execute-plan.md
@/home/jta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-wizard-persistence/02-CONTEXT.md
@packages/front/src/lib/tournament/components/PublishStep.svelte
@packages/front/src/routes/(app)/events/+page.svelte
@packages/front/src/lib/tournament/types.ts
</context>

<interfaces>
<!-- PublishStep Props mis à jour (supprimer options/PublishOptions, ajouter eventStatus) -->
interface Props {
  event: EventData
  tournaments: Tournament[]
  // options: PublishOptions   ← SUPPRIMER ce prop
  onPrev: () => void
  onPublish: () => void
  publishError?: string
  eventStatus?: 'draft' | 'ready' | 'started'  // NOUVEAU — conditionne le bouton Publier
}

<!-- Logique du bouton Publier dans PublishStep -->
// Si eventStatus === 'draft' OU undefined → afficher le bouton "Publier"
// Si eventStatus === 'ready' ou 'started' → afficher uniquement "Enregistrer" (onSave prop ?)
//   → Alternative : ne pas afficher le bouton Publier, le bouton Enregistrer est dans le header wizard
//   → Décision Claude : PublishStep si ready/started = récapitulatif seul + message "Événement publié"
//                        + pas de bouton Publier (le bouton Enregistrer global dans le header suffit)

<!-- /events +page.svelte — logique des liens d'édition -->
// Actuellement : si event.status === 'draft' → afficher "Reprendre l'édition"
// Mis à jour : si event.status !== 'finished' → afficher lien d'édition
//   - status 'draft' → texte "Reprendre l'édition →"
//   - status 'ready' → texte "Modifier →"
//   - status 'started' → texte "Modifier →" (EventStep sera readonly côté wizard)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactorer PublishStep — supprimer checkboxes, conditionner le bouton Publier</name>
  <files>packages/front/src/lib/tournament/components/PublishStep.svelte</files>
  <action>
Modifier `PublishStep.svelte` :

1. **Supprimer l'import `PublishOptions`** de la ligne d'import des types.
2. **Supprimer le prop `options`** (type PublishOptions) de l'interface Props et de la déstructuration `$props()`.
3. **Ajouter le prop `eventStatus?: 'draft' | 'ready' | 'started'`** à l'interface Props.
4. **Supprimer le bloc `<Card>` "Options de publication"** entier (la Card qui contient les deux Checkbox Flowbite) — lignes 111-122 environ dans le fichier actuel.
5. **Supprimer l'import `Checkbox`** de flowbite-svelte (devenu inutile).
6. **Conditionner le bouton Publier** dans la section Actions :
   - Si `eventStatus === undefined || eventStatus === 'draft'` → afficher le bouton "Publier" (comportement actuel)
   - Si `eventStatus === 'ready' || eventStatus === 'started'` → ne pas afficher le bouton Publier. Afficher à la place un message informatif : `<p class="text-sm text-gray-500">Cet événement est publié. Utilisez "Enregistrer" pour mettre à jour le contenu.</p>`
7. **Le bouton "← Modifier"** (onPrev) reste toujours visible.
8. **Supprimer `bind:options`** des deux pages wizard qui appellent PublishStep (`events/new/+page.svelte` et `events/[id]/edit/+page.svelte`) — supprimer la prop `bind:options={publishOptions}` et la variable `publishOptions` dans le script de chaque page. Supprimer aussi l'import `PublishOptions` du type dans ces pages. NB : ces fichiers sont peut-être déjà mis à jour par le plan 02-04 pour edit/+page.svelte — vérifier et ne modifier que ce qui reste.
  </action>
  <verify>
    <automated>cd /home/jta/Projects/darts-management/master && grep -n "PublishOptions\|Checkbox\|openRegistrations\|notifications" packages/front/src/lib/tournament/components/PublishStep.svelte | wc -l</automated>
  </verify>
  <done>grep retourne 0 — PublishOptions, Checkbox, openRegistrations, notifications n'apparaissent plus dans PublishStep.svelte. Le fichier contient "eventStatus". pnpm typecheck passe.</done>
</task>

<task type="auto">
  <name>Task 2: Mettre à jour /events list — liens d'édition pour ready et started</name>
  <files>packages/front/src/routes/(app)/events/+page.svelte</files>
  <action>
Modifier `packages/front/src/routes/(app)/events/+page.svelte` :

Actuellement (lignes 59-68) :
```svelte
{#if event.status === 'draft'}
  <div class="mt-3 border-t border-gray-100 pt-3">
    <a href="/events/{event.id}/edit" class="text-sm font-medium text-blue-600 hover:text-blue-800">
      Reprendre l'édition →
    </a>
  </div>
{/if}
```

Remplacer par une logique qui affiche un lien d'édition pour tous les statuts sauf `finished` :
```svelte
{#if event.status !== 'finished'}
  <div class="mt-3 border-t border-gray-100 pt-3">
    <a href="/events/{event.id}/edit" class="text-sm font-medium text-blue-600 hover:text-blue-800">
      {event.status === 'draft' ? 'Reprendre l\'édition →' : 'Modifier →'}
    </a>
  </div>
{/if}
```

Aucun autre changement dans ce fichier.
  </action>
  <verify>
    <automated>cd /home/jta/Projects/darts-management/master && grep -n "status !== 'finished'\|Modifier" packages/front/src/routes/\(app\)/events/+page.svelte</automated>
  </verify>
  <done>La liste /events affiche un lien d'édition conditionné sur status !== 'finished', avec texte "Reprendre l'édition" pour draft et "Modifier" pour ready/started.</done>
</task>

</tasks>

<verification>
```bash
# PublishStep sans checkboxes
grep "Checkbox\|openRegistrations\|notifications\|PublishOptions" \
  /home/jta/Projects/darts-management/master/packages/front/src/lib/tournament/components/PublishStep.svelte
# doit retourner vide

# eventStatus présent dans PublishStep
grep "eventStatus" \
  /home/jta/Projects/darts-management/master/packages/front/src/lib/tournament/components/PublishStep.svelte

# Lien d'édition pour tous statuts non-finished
grep "finished\|Modifier" \
  /home/jta/Projects/darts-management/master/packages/front/src/routes/\(app\)/events/+page.svelte

# typecheck global (après completion de tous les plans wave 2)
cd /home/jta/Projects/darts-management/master && pnpm typecheck
```
</verification>

<success_criteria>
- PublishStep.svelte ne contient plus les 2 Checkbox, ni l'import Checkbox, ni PublishOptions
- PublishStep.svelte affiche le bouton Publier uniquement si eventStatus === 'draft' ou undefined
- /events list affiche "Modifier →" pour les événements ready et started
- /events list affiche "Reprendre l'édition →" pour les événements draft
- pnpm typecheck passe sans erreur une fois tous les plans wave 2 terminés
</success_criteria>

<output>
After completion, create `.planning/phases/02-wizard-persistence/02-05-SUMMARY.md`
</output>
