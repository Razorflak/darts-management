---
phase: 03.2-player-profile
plan: "03"
subsystem: frontend-ui
tags: [player-profile, registration-gating, navbar, dashboard, event-page]
dependency_graph:
  requires: [03.2-01]
  provides: [player-profile-ui-gating]
  affects: [packages/front/src/routes/(app)/+layout.svelte, packages/front/src/routes/(app)/+page.svelte, packages/front/src/routes/(app)/events/[id]/+page.svelte]
tech_stack:
  added: []
  patterns: [SvelteKit layout data propagation, Svelte 5 conditional rendering, Flowbite-Svelte DropdownItem href]
key_files:
  created: []
  modified:
    - packages/front/src/routes/(app)/+layout.svelte
    - packages/front/src/routes/(app)/+page.svelte
    - packages/front/src/routes/(app)/events/[id]/+page.svelte
decisions:
  - "disabled={!data.player} on S'inscrire buttons only — Se désinscrire intentionally left enabled so already-registered users can still withdraw"
  - "data.player propagated from (app)/+layout.server.ts — no additional server queries needed in child pages"
metrics:
  duration: "3 min"
  completed_date: "2026-03-13"
  tasks_completed: 2
  files_modified: 3
---

# Phase 03.2 Plan 03: Player Profile UI Gating Summary

Wire data.player into consumer UI — navbar Mon profil link, dashboard orange warning tile, event page red banner with disabled registration buttons.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Add Mon profil to navbar dropdown + no-profile dashboard tile | 41eaee8 | +layout.svelte, +page.svelte |
| 2 | Add no-profile banner and disabled registration buttons to events/[id]/+page.svelte | 8514ec5 | events/[id]/+page.svelte |

## What Was Built

**+layout.svelte (navbar):** Added `<DropdownItem href="/profile">Mon profil</DropdownItem>` with a new `<DropdownDivider />` between the profile link and the Se déconnecter item. Visible for all authenticated users.

**+page.svelte (dashboard):** Orange dashed tile inserted between the header section and the tournament grid. Renders only when `data.user && !data.player`. Contains a user profile SVG icon, heading "Compléter mon profil joueur", descriptive text, and a "Créer mon profil →" link to `/profile/create`.

**events/[id]/+page.svelte (event page):** Three changes:
1. Added `import { page } from "$app/stores"` for URL-aware redirectTo construction
2. Red banner after event header when `data.user && !data.player` — links to `/profile/create?redirectTo={encodeURIComponent($page.url.pathname)}`
3. `disabled={!data.player}` added to S'inscrire and S'inscrire (doubles) buttons; Se désinscrire button intentionally unaffected

## Deviations from Plan

None — plan executed exactly as written.

## Verification

TypeScript check: 4 pre-existing errors in admin files (RegistrationModal.svelte, admin +page.svelte), all unrelated to this plan's changes. Zero new errors introduced.

## Self-Check: PASSED

- +layout.svelte: FOUND
- +page.svelte: FOUND
- events/[id]/+page.svelte: FOUND
- Commit 41eaee8: FOUND (navbar + dashboard tile)
- Commit 8514ec5: FOUND (event page banner + disabled buttons)
