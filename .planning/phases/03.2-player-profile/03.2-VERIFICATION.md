---
phase: 03.2-player-profile
verified: 2026-03-13T10:00:00Z
status: passed
score: 10/10 must-haves verified
re_verification: false
gaps: []
human_verification:
  - test: "Visiter /profile/create en tant qu'utilisateur sans profil"
    expected: "Le formulaire s'affiche correctement avec les champs Prénom, Nom, Département, Date de naissance, Numéro de licence. Soumission crée le profil et redirige vers safeRedirectTo."
    why_human: "Comportement de formulaire SvelteKit avec enhance, redirection post-action, valeur de redirectTo transmise via input caché"
  - test: "Visiter la page événement en tant qu'utilisateur sans profil"
    expected: "Banner rouge visible avec lien /profile/create?redirectTo=..., boutons S'inscrire et S'inscrire (doubles) désactivés visuellement, bouton Se désinscrire non affecté"
    why_human: "État disabled sur composant Flowbite Button — comportement visuel à confirmer"
  - test: "Tableau de bord sans profil"
    expected: "Tuile orange dashed visible entre le header et la grille des tournois, disparaît après création du profil (navigation)"
    why_human: "Réactivité SvelteKit après navigation post-création de profil"
  - test: "Profil avec numéro de licence"
    expected: "Bouton Modifier mon profil absent sur /profile, /profile/edit redirige immédiatement vers /profile en load ET en action directe POST"
    why_human: "Guard licence_no dans les deux chemins (load + action) — nécessite test avec requête POST directe pour confirmer le bypass guard"
---

# Phase 03.2: Player Profile Verification Report

**Phase Goal:** Users without a player profile are gated — they cannot register for tournaments and are directed to create their profile first.
**Verified:** 2026-03-13T10:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Utilisateur connecté sans profil — `locals.player = null` (plus d'auto-création) | VERIFIED | `hooks.server.ts` ligne 30 : `event.locals.player = rows.length > 0 ? PlayerSchema.parse(rows[0]) : null` — aucun INSERT INTO player |
| 2 | `data.player` disponible dans toutes les pages `(app)` via propagation layout | VERIFIED | `(app)/+layout.server.ts` ligne 17 : `player: locals.player` dans le return |
| 3 | Utilisateurs avec profil existant non affectés — `locals.player` retourne leur profil normalement | VERIFIED | Même chemin SELECT + parse — aucune régression logique |
| 4 | Utilisateur sans profil peut accéder à `/profile/create` et créer son profil | VERIFIED | `create/+page.server.ts` : guard redirect si profil existant, INSERT INTO player dans l'action, safeRedirectTo |
| 5 | Après création, redirection vers `safeRedirectTo` ou `/` | VERIFIED | `create/+page.server.ts` lignes 40-42 : safeRedirectTo vérifié via `startsWith("/")` |
| 6 | Utilisateur avec profil visitant `/profile/create` redirigé vers `/profile` | VERIFIED | `create/+page.server.ts` ligne 8 : `if (locals.player) redirect(302, "/profile")` |
| 7 | `/profile` affiche les champs du profil, bouton Modifier absent si `licence_no` présent | VERIFIED | `profile/+page.svelte` : `{#if data.player.licence_no === null}` conditionne le bouton |
| 8 | `/profile/edit` inaccessible aux utilisateurs avec `licence_no` (load ET action) | VERIFIED | `edit/+page.server.ts` : guard dans `load()` ligne 9 ET dans `actions.default()` ligne 17 |
| 9 | Banner rouge sur la page événement avec lien `/profile/create?redirectTo=...` | VERIFIED | `events/[id]/+page.svelte` lignes 91-101 : `{#if data.user && !data.player}` + `encodeURIComponent($page.url.pathname)` |
| 10 | Boutons S'inscrire désactivés quand `data.player` est null, Se désinscrire non affecté | VERIFIED | `events/[id]/+page.svelte` lignes 160, 169 : `disabled={!data.player}` sur les deux boutons d'inscription uniquement |

**Score:** 10/10 truths verified

---

### Required Artifacts

| Artifact | Provides | Status | Details |
|----------|----------|--------|---------|
| `packages/front/src/hooks.server.ts` | Lookup-only, `locals.player = parse\|null` | VERIFIED | Ligne 30 : pattern exact. Aucun INSERT. Commit d9764d3. |
| `packages/front/src/routes/(app)/+layout.server.ts` | `player: locals.player` dans le return | VERIFIED | Ligne 17. Commit 8e9966c. |
| `packages/front/src/lib/server/schemas/event-schemas.ts` | `CreateProfileSchema`, `UpdateProfileSchema` exportés | VERIFIED | Lignes 254-270. Types `CreateProfileInput`, `UpdateProfileInput` dérivés via `z.infer`. Commit 0bded04. |
| `packages/front/src/lib/tournament/components/PlayerCreationForm.svelte` | Composant champs-uniquement, prop `showLicence` | VERIFIED | 89 lignes. Pas de balise `<form>` — les pages contrôlent le form element. Props `values`, `submitLabel`, `showLicence`, `form`. Commit a51bf29. |
| `packages/front/src/routes/(app)/profile/+page.server.ts` | Guard + retourne `player` | VERIFIED | Redirect `/login` si pas d'user, redirect `/profile/create` si pas de profil. |
| `packages/front/src/routes/(app)/profile/+page.svelte` | Affichage read-only, bouton Modifier conditionnel | VERIFIED | Tous champs affichés. `{#if data.player.licence_no === null}` pour le bouton. |
| `packages/front/src/routes/(app)/profile/create/+page.server.ts` | Load guard + action INSERT + safeRedirectTo | VERIFIED | `export load` + `export actions`. INSERT INTO player dans l'action. Double sanitization safeRedirectTo. |
| `packages/front/src/routes/(app)/profile/create/+page.svelte` | Form avec redirectTo caché, PlayerCreationForm | VERIFIED | `<input type="hidden" name="redirectTo">` dans le form, `<PlayerCreationForm showLicence={true}>`. |
| `packages/front/src/routes/(app)/profile/edit/+page.server.ts` | Load guard + action guard `licence_no` + UPDATE | VERIFIED | Double guard dans load ET action. `UPDATE player SET ... WHERE id = locals.player.id`. |
| `packages/front/src/routes/(app)/profile/edit/+page.svelte` | Form PlayerCreationForm, `showLicence={false}`, pré-rempli | VERIFIED | `showLicence={false}`. Values pré-peuplées depuis `data.player`. |
| `packages/front/src/routes/(app)/+layout.svelte` | DropdownItem "Mon profil" → `/profile` | VERIFIED | Ligne 39 : `<DropdownItem href="/profile">Mon profil</DropdownItem>`. |
| `packages/front/src/routes/(app)/+page.svelte` | Tuile orange si `data.user && !data.player` | VERIFIED | Lignes 62-77 : tuile dashed orange avec lien `/profile/create`. |
| `packages/front/src/routes/(app)/events/[id]/+page.svelte` | Banner rouge + boutons disabled | VERIFIED | Banner lignes 91-101. `disabled={!data.player}` lignes 160, 169. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `hooks.server.ts` | `event.locals.player` | `SELECT FROM player WHERE user_id = userId` | WIRED | Pattern `rows.length > 0 ? PlayerSchema.parse(rows[0]) : null` confirmé, aucun INSERT |
| `(app)/+layout.server.ts` | `data.player` | `return { ..., player: locals.player }` | WIRED | Ligne 17 confirmée |
| `profile/create/+page.svelte` | `PlayerCreationForm.svelte` | `import PlayerCreationForm` | WIRED | Ligne 4 : `import PlayerCreationForm from "$lib/tournament/components/PlayerCreationForm.svelte"` |
| `profile/create/+page.server.ts` | table `player` | `INSERT INTO player` | WIRED | Lignes 33-38 : INSERT complet avec tous les champs |
| `profile/edit/+page.server.ts` | table `player` | `UPDATE player SET` | WIRED | Lignes 33-40 : UPDATE avec guard `locals.player.id` |
| `events/[id]/+page.svelte` | `data.player` | propagation layout SvelteKit | WIRED | `data.player` utilisé lignes 91, 160, 169 — propagé depuis `(app)/+layout.server.ts` |
| `(app)/+page.svelte` | `data.player` | propagation layout SvelteKit | WIRED | `data.player` utilisé ligne 62 — propagé depuis `(app)/+layout.server.ts` |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PROFILE-01 | 03.2-01, 03.2-03 | Player state global dans le layout — `data.player` disponible partout + gating UI (banner, tuile, boutons disabled) | SATISFIED | `+layout.server.ts` retourne `player`, `+page.svelte` tuile, `events/[id]/+page.svelte` banner + disabled |
| PROFILE-02 | 03.2-01, 03.2-02 | hooks lookup-only (no auto-create) + pages de profil `/profile`, `/profile/create`, `/profile/edit` | SATISFIED | `hooks.server.ts` sans INSERT, 3 routes créées avec guards et actions SQL |
| PROFILE-03 | 03.2-03 | Registration gating — S'inscrire désactivé sans profil, Se désinscrire non affecté | SATISFIED | `disabled={!data.player}` sur les boutons d'inscription uniquement |

**Note sur REQUIREMENTS.md :** Les identifiants PROFILE-01, PROFILE-02, PROFILE-03 sont référencés dans le ROADMAP.md et les plans de phase mais ne sont pas définis dans `.planning/REQUIREMENTS.md`. Ce fichier couvre uniquement les requirements v1 initiaux (AUTH, ORG, EVENT, PLAYER, LAUNCH, RESULT, VIEW). La Phase 03.2 a été insérée après la rédaction initiale des requirements — les IDs PROFILE sont des exigences de phase valides documentées dans le ROADMAP mais absentes du registre REQUIREMENTS.md. Il n'y a pas d'exigences PROFILE orphelines à signaler depuis REQUIREMENTS.md.

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `PlayerCreationForm.svelte` lignes 40, 52, 81 | `placeholder="..."` (HTML input attribute) | Info | Non concerné — attributs HTML natifs, pas des stubs de code |

Aucun anti-pattern réel détecté. Aucun TODO/FIXME/placeholder de code. Aucune implémentation stub (return null, return {}, handlers vides). Toutes les actions SQL sont complètes.

---

### Human Verification Required

#### 1. Flux de création de profil complet

**Test:** Se connecter avec un compte sans profil player, visiter `/profile/create`, remplir et soumettre le formulaire.
**Expected:** Profil créé en base, redirection vers `/` ou la valeur de `redirectTo` si présente dans l'URL.
**Why human:** Comportement post-action SvelteKit avec `use:enhance`, transmission du `redirectTo` via input caché, validation du `safeRedirectTo`.

#### 2. Gating visuel sur la page événement

**Test:** Se connecter sans profil, visiter une page `/events/[id]`.
**Expected:** Banner rouge visible avec lien pointant vers `/profile/create?redirectTo=%2Fevents%2F[id]`. Boutons S'inscrire et S'inscrire (doubles) affichés mais désactivés visuellement. Bouton Se désinscrire (si applicable) non désactivé.
**Why human:** État `disabled` sur composant Flowbite Button — apparence et comportement visuel non vérifiables statiquement.

#### 3. Disparition de la tuile dashboard après création

**Test:** Créer un profil depuis la tuile dashboard, constater la navigation retour.
**Expected:** La tuile orange disparaît — `data.player` est non null après la navigation post-création.
**Why human:** Réactivité SvelteKit lors de la revalidation des données layout après une action de page.

#### 4. Guard `licence_no` sur `/profile/edit` via POST direct

**Test:** Avec un profil ayant un `licence_no`, envoyer une requête POST directe à `/profile/edit?/default`.
**Expected:** Redirection 303 vers `/profile` — le guard dans `actions.default()` est déclenché.
**Why human:** Nécessite un outil type cURL ou Postman avec la session active pour tester le contournement du guard load.

---

### Gaps Summary

Aucun gap détecté. Toutes les truths sont vérifiées, tous les artefacts existent avec une implémentation substantielle et sont correctement câblés.

---

_Verified: 2026-03-13T10:00:00Z_
_Verifier: Claude (gsd-verifier)_
