---
phase: 04-launch-and-match-generation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/016_match.sql
  - packages/front/src/lib/server/schemas/event-schemas.ts
autonomous: true
requirements:
  - LAUNCH-01
  - LAUNCH-04

must_haves:
  truths:
    - "La table `match` existe en base avec tous les champs requis (phase_id, event_match_id, team_a_id, team_b_id, advances_to_match_id, sets_to_win, legs_per_set, status)"
    - "La table `phase` a les colonnes `sets_to_win` et `legs_per_set`"
    - "La table `tournament` a les colonnes `is_seeded` et `seed_order`"
    - "Les schemas Zod MatchSchema, LaunchPreviewSchema, AdminTournamentDetailSchema sont exportés depuis event-schemas.ts"
  artifacts:
    - path: "packages/db/src/schema/016_match.sql"
      provides: "Table match + colonnes tournament.is_seeded/seed_order + colonnes phase.sets_to_win/legs_per_set"
      contains: "CREATE TABLE match"
    - path: "packages/front/src/lib/server/schemas/event-schemas.ts"
      provides: "MatchSchema, LaunchPreviewSchema, GroupPhaseSchema/EliminationPhaseSchema étendus avec sets_to_win/legs_per_set"
      exports: ["MatchSchema", "Match", "LaunchPreviewSchema", "LaunchPreview"]
  key_links:
    - from: "packages/db/src/schema/016_match.sql"
      to: "packages/front/src/lib/server/schemas/event-schemas.ts"
      via: "MatchSchema reflects match table columns exactly"
      pattern: "MatchSchema"
---

<objective>
Migration DB 016 : table match + extension tournament/phase + schemas Zod Phase 4.

Purpose: Poser la fondation schema pour toute la génération de matchs. La table `match` est le seul ajout DB de Phase 4. Les colonnes `sets_to_win`/`legs_per_set` sur `phase` permettent LAUNCH-04. Les colonnes `is_seeded`/`seed_order` sur `tournament` permettent l'option seeding. Les schemas Zod derivent de cette migration et servent de contrats pour tous les plans suivants.
Output: Migration SQL exécutable + types TypeScript Zod-first pour match, preview lancement.
</objective>

<execution_context>
@/home/jta/.claude/get-shit-done/workflows/execute-plan.md
@/home/jta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/front/src/lib/server/schemas/event-schemas.ts
@packages/db/src/schema/009_align_schemas.sql
@packages/db/src/schema/013_teams.sql

<interfaces>
<!-- Schema actuel pertinent — colonnes existantes sur les tables à modifier -->

Phase table actuelle (008 + 009) :
```sql
phase(id UUID, tournament_id UUID, position INTEGER, type TEXT,
      players_per_group INTEGER, qualifiers_per_group INTEGER,
      qualifiers_count INTEGER, tiers JSONB, created_at TIMESTAMPTZ)
```

Tournament table actuelle (007 + 009 + 014) :
```sql
tournament(id UUID, event_id UUID, name TEXT, category TEXT,
           start_at TIMESTAMPTZ, auto_referee BOOLEAN,
           check_in_required BOOLEAN,
           status TEXT -- 'ready'|'check-in'|'started'|'finished',
           created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ)
```

Team table actuelle (013) :
```sql
team(id UUID, created_at TIMESTAMPTZ)
tournament_registration(id UUID, tournament_id UUID, team_id UUID, checked_in BOOLEAN, registered_at TIMESTAMPTZ)
```

PhaseSchema actuel — GroupPhaseSchema et EliminationPhaseSchema dans event-schemas.ts :
```typescript
export const GroupPhaseSchema = CommonPhaseSchema.extend({
  type: z.enum(["round_robin", "double_loss_groups"]),
  players_per_group: z.number().int(),
  qualifiers_per_group: z.number().int(),
})

export const EliminationPhaseSchema = CommonPhaseSchema.extend({
  type: z.enum(["single_elimination", "double_elimination"]),
  tiers: z.array(BracketTierSchema).min(1),
  qualifiers_count: z.number().int().nonnegative(),
})
```

TournamentSchema actuel :
```typescript
export const TournamentSchema = z.object({
  id: z.uuid(),
  name: z.string(),
  category: CategorySchema,
  start_at: z.coerce.date().nullable(),
  phases: z.array(PhaseSchema).min(1),
  auto_referee: z.boolean(),
  check_in_required: z.boolean(),
  status: z.enum(["ready", "check-in", "started", "finished"]),
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 016 — table match + colonnes tournament/phase</name>
  <files>packages/db/src/schema/016_match.sql</files>
  <action>
Créer `packages/db/src/schema/016_match.sql` avec :

**Table match :**
```sql
CREATE TABLE match (
  id                   UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  phase_id             UUID NOT NULL REFERENCES phase(id) ON DELETE CASCADE,
  event_match_id       INTEGER NOT NULL,
  group_number         INTEGER,
  round_number         INTEGER NOT NULL DEFAULT 0,
  position             INTEGER NOT NULL DEFAULT 0,
  team_a_id            UUID REFERENCES team(id) ON DELETE SET NULL,
  team_b_id            UUID REFERENCES team(id) ON DELETE SET NULL,
  referee_team_id      UUID REFERENCES team(id) ON DELETE SET NULL,
  advances_to_match_id UUID REFERENCES match(id) ON DELETE SET NULL,
  score_a              INTEGER,
  score_b              INTEGER,
  status               TEXT NOT NULL DEFAULT 'pending',
  sets_to_win          INTEGER NOT NULL DEFAULT 2,
  legs_per_set         INTEGER NOT NULL DEFAULT 3,
  created_at           TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX match_phase_idx ON match(phase_id);
CREATE INDEX match_event_match_id_idx ON match(event_match_id);
```

**Colonnes tournament :**
```sql
ALTER TABLE tournament ADD COLUMN is_seeded   BOOLEAN NOT NULL DEFAULT false;
ALTER TABLE tournament ADD COLUMN seed_order  JSONB   NOT NULL DEFAULT '[]';
```

**Colonnes phase :**
```sql
ALTER TABLE phase ADD COLUMN sets_to_win  INTEGER NOT NULL DEFAULT 2;
ALTER TABLE phase ADD COLUMN legs_per_set INTEGER NOT NULL DEFAULT 3;
```

Puis exécuter la migration : `pnpm db:migrate`
  </action>
  <verify>
    <automated>cd /home/jta/Projects/darts-management/master && pnpm db:migrate 2>&1 | tail -5</automated>
  </verify>
  <done>Migration 016 appliquée sans erreur. Colonnes `is_seeded`, `seed_order` présentes sur tournament. Colonnes `sets_to_win`, `legs_per_set` présentes sur phase et match.</done>
</task>

<task type="auto">
  <name>Task 2: Schemas Zod Phase 4 — MatchSchema + extensions phase/tournament</name>
  <files>packages/front/src/lib/server/schemas/event-schemas.ts</files>
  <action>
Modifier `packages/front/src/lib/server/schemas/event-schemas.ts` pour ajouter les types Phase 4.

**1. Étendre GroupPhaseSchema** avec `sets_to_win` et `legs_per_set` :
```typescript
export const GroupPhaseSchema = CommonPhaseSchema.extend({
  type: z.enum(["round_robin", "double_loss_groups"]),
  players_per_group: z.number().int(),
  qualifiers_per_group: z.number().int(),
  sets_to_win: z.number().int().positive().default(2),
  legs_per_set: z.number().int().positive().default(3),
})
```

**2. Étendre EliminationPhaseSchema** avec `sets_to_win` et `legs_per_set` (pour le défaut phase-level, même si les tiers overrident par round) :
```typescript
export const EliminationPhaseSchema = CommonPhaseSchema.extend({
  type: z.enum(["single_elimination", "double_elimination"]),
  tiers: z.array(BracketTierSchema).min(1),
  qualifiers_count: z.number().int().nonnegative(),
  sets_to_win: z.number().int().positive().default(2),
  legs_per_set: z.number().int().positive().default(3),
})
```

**3. Étendre TournamentSchema** avec `is_seeded` et `seed_order` :
```typescript
export const TournamentSchema = z.object({
  id: z.uuid(),
  name: z.string(),
  category: CategorySchema,
  start_at: z.coerce.date().nullable(),
  phases: z.array(PhaseSchema).min(1),
  auto_referee: z.boolean(),
  check_in_required: z.boolean(),
  status: z.enum(["ready", "check-in", "started", "finished"]),
  is_seeded: z.boolean(),
  seed_order: z.array(z.uuid()),
})
```
Mettre à jour aussi `DraftTournamentSchema` pour inclure ces deux champs avec defaults.

**4. Ajouter MatchSchema** en section `### MATCH ###` après les schemas tournament :
```typescript
export const MatchStatusSchema = z.enum(["pending", "ongoing", "done", "walkover"])
export type MatchStatus = z.infer<typeof MatchStatusSchema>

export const MatchSchema = z.object({
  id: z.uuid(),
  phase_id: z.uuid(),
  event_match_id: z.number().int(),
  group_number: z.number().int().nullable(),
  round_number: z.number().int(),
  position: z.number().int(),
  team_a_id: z.uuid().nullable(),
  team_b_id: z.uuid().nullable(),
  referee_team_id: z.uuid().nullable(),
  advances_to_match_id: z.uuid().nullable(),
  score_a: z.number().int().nullable(),
  score_b: z.number().int().nullable(),
  status: MatchStatusSchema,
  sets_to_win: z.number().int(),
  legs_per_set: z.number().int(),
  created_at: z.coerce.date(),
})
export type Match = z.infer<typeof MatchSchema>
```

**5. Ajouter MatchInsertRowSchema** (utilisé par les generators, sans `id`/`created_at`) :
```typescript
export const MatchInsertRowSchema = MatchSchema.omit({ created_at: true }).partial({
  id: true,
  score_a: true,
  score_b: true,
  group_number: true,
  team_a_id: true,
  team_b_id: true,
  referee_team_id: true,
  advances_to_match_id: true,
})
export type MatchInsertRow = z.infer<typeof MatchInsertRowSchema>
```

**6. Ajouter LaunchPreviewSchema** (utilisé par la page /launch) :
```typescript
export const LaunchGroupPreviewSchema = z.object({
  group_number: z.number().int(),
  group_name: z.string(),
  team_count: z.number().int(),
  match_count: z.number().int(),
})

export const LaunchPhasePreviewSchema = z.object({
  phase_id: z.uuid(),
  phase_type: PhaseTypeSchema,
  phase_position: z.number().int(),
  match_count: z.number().int(),
  groups: z.array(LaunchGroupPreviewSchema),
})

export const LaunchPreviewSchema = z.object({
  tournament_id: z.uuid(),
  tournament_name: z.string(),
  registered_count: z.number().int(),
  checked_in_count: z.number().int(),
  active_count: z.number().int(), // will participate (checked-in si check_in_required)
  check_in_required: z.boolean(),
  phases: z.array(LaunchPhasePreviewSchema),
  total_match_count: z.number().int(),
})
export type LaunchPreview = z.infer<typeof LaunchPreviewSchema>
export type LaunchPhasePreview = z.infer<typeof LaunchPhasePreviewSchema>
export type LaunchGroupPreview = z.infer<typeof LaunchGroupPreviewSchema>
```

Vérifier le typecheck après modification.
  </action>
  <verify>
    <automated>cd /home/jta/Projects/darts-management/master && pnpm typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>pnpm typecheck passe. MatchSchema, LaunchPreviewSchema exportés depuis event-schemas.ts. GroupPhaseSchema/EliminationPhaseSchema ont sets_to_win et legs_per_set. TournamentSchema a is_seeded et seed_order.</done>
</task>

</tasks>

<verification>
- `pnpm db:migrate` passe sans erreur
- `pnpm typecheck` passe
- `pnpm lint` passe
- Table match présente en DB (psql ou migration output confirme)
- event-schemas.ts exporte MatchSchema, LaunchPreviewSchema, Match, LaunchPreview
</verification>

<success_criteria>
Migration 016 appliquée. Schemas Zod Phase 4 en place. Tout le code existant continue de compiler (les nouveaux champs `is_seeded`, `seed_order`, `sets_to_win`, `legs_per_set` ont des valeurs par défaut sur les schemas et en DB).
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-and-match-generation/04-01-SUMMARY.md`
</output>
