---
phase: 01-foundation
plan: 06
type: execute
wave: 2
depends_on:
  - "01-05"
files_modified:
  - packages/front/src/routes/(app)/admin/+page.server.ts
  - packages/front/src/routes/(app)/admin/entities/new/+page.server.ts
  - packages/front/src/lib/auth-client.ts
  - packages/front/src/routes/(app)/+layout.server.ts
  - packages/front/src/routes/(app)/+layout.svelte
autonomous: true
gap_closure: true
requirements:
  - AUTH-04
  - ORG-01
  - ORG-02
  - ORG-03

must_haves:
  truths:
    - "Un admin fédéral peut accéder à /admin (garde basée sur user_entity_role, pas sur auth.api.userHasPermission)"
    - "Un utilisateur sans rôle adminFederal dans user_entity_role reçoit une 403 en accédant à /admin"
    - "Le formulaire de création d'entité vérifie les droits via checkRole avant d'INSERTer"
    - "Le layout server calcule hasAdminAccess via getUserRoles et le transmet au layout Svelte qui conditionne le lien Admin sur data.hasAdminAccess"
    - "Le typecheck passe sur packages/front sans erreur liée aux imports du plugin admin"
  artifacts:
    - path: "packages/front/src/routes/(app)/admin/+page.server.ts"
      provides: "Guard avec checkRole(userId, entityId, adminFederal)"
      contains: "checkRole"
    - path: "packages/front/src/routes/(app)/admin/entities/new/+page.server.ts"
      provides: "Action CREATE avec checkRole"
      contains: "checkRole"
    - path: "packages/front/src/routes/(app)/+layout.server.ts"
      provides: "Load function retournant hasAdminAccess calculé via getUserRoles"
      contains: "hasAdminAccess"
  key_links:
    - from: "packages/front/src/routes/(app)/admin/+page.server.ts"
      to: "packages/db/src/authz.ts"
      via: "import { checkRole } from '@darts-management/db'"
      pattern: "checkRole"
    - from: "packages/front/src/routes/(app)/+layout.server.ts"
      to: "packages/db/src/authz.ts"
      via: "getUserRoles pour déterminer si lien Admin doit être affiché"
      pattern: "getUserRoles"
    - from: "packages/front/src/routes/(app)/+layout.svelte"
      to: "packages/front/src/routes/(app)/+layout.server.ts"
      via: "data.hasAdminAccess conditionne l'affichage du lien Admin dans la navbar"
      pattern: "data\\.hasAdminAccess"
---

<objective>
Mettre à jour tous les gardes de permission dans le frontend pour utiliser le nouveau module `authz` (checkRole/getUserRoles) au lieu de `auth.api.userHasPermission` du plugin admin supprimé.

Purpose: Les appels à `auth.api.userHasPermission` vont échouer au runtime car le plugin admin est retiré. Les gardes frontend doivent être réécrits pour interroger directement `user_entity_role` via le module authz.
Output: Gardes admin réécrites avec checkRole, layout server mis à jour pour passer les rôles, navbar conditionnelle corrigée.
</objective>

<execution_context>
@/home/jta/.claude/get-shit-done/workflows/execute-plan.md
@/home/jta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-05-SUMMARY.md

<interfaces>
<!-- Nouveau module authz — produit par plan 01-05 -->
From packages/db/src/authz.ts (créé en 01-05):
```typescript
export type EntityRole =
  | "organisateur" | "adminTournoi" | "adminClub"
  | "adminComite" | "adminLigue" | "adminFederal"

export async function checkRole(
  userId: string,
  entityId: string,
  role: EntityRole,
): Promise<boolean>

export async function getUserRoles(
  userId: string,
): Promise<Array<{ entityId: string; role: EntityRole }>>

export async function canPromote(
  promoterId: string,
  entityId: string,
  targetRole: EntityRole,
): Promise<boolean>
```

<!-- Pattern authz actuel (à remplacer) dans admin/+page.server.ts -->
<!-- AVANT (à supprimer) : -->
```typescript
const canManage = await auth.api.userHasPermission({
  body: { userId: locals.user.id, permissions: { entity: ["create"] } },
})
if (!canManage.success) error(403, "Accès réservé aux administrateurs fédéraux.")
```
<!-- APRÈS : -->
```typescript
import { sql, checkRole, getUserRoles } from "@darts-management/db"

// Pour /admin : l'utilisateur doit avoir adminFederal sur AU MOINS UNE entité
// (l'adminFederal a son rôle scopé sur la fédération dans user_entity_role)
const userRoles = await getUserRoles(locals.user.id)
const isAdminFederal = userRoles.some(r => r.role === "adminFederal")
if (!isAdminFederal) error(403, "Accès réservé aux administrateurs fédéraux.")
```

<!-- app.d.ts (existant — ne pas modifier) -->
```typescript
interface Locals {
  user: User | null
  session: Session | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Réécrire les gardes admin — +page.server.ts et entities/new/+page.server.ts</name>
  <files>
    packages/front/src/routes/(app)/admin/+page.server.ts
    packages/front/src/routes/(app)/admin/entities/new/+page.server.ts
  </files>
  <action>
**Lire d'abord les deux fichiers existants** pour comprendre leur structure actuelle avant de les modifier.

**admin/+page.server.ts** — remplacer la vérification de permission :
- Supprimer l'import de `auth` depuis `$lib/server/auth` si il n'est plus utilisé après le changement
- Ajouter l'import : `import { sql, getUserRoles } from "@darts-management/db"`
- Remplacer l'appel `auth.api.userHasPermission(...)` par :
  ```typescript
  const userRoles = await getUserRoles(locals.user.id)
  const isAdminFederal = userRoles.some(r => r.role === "adminFederal")
  if (!isAdminFederal) error(403, "Accès réservé aux administrateurs fédéraux.")
  ```
- Conserver la requête SQL existante pour récupérer les entités (ne pas la modifier)

**admin/entities/new/+page.server.ts** — remplacer la vérification dans load ET dans l'action :
- Ajouter l'import : `import { sql, getUserRoles } from "@darts-management/db"`
- Dans `load` : même pattern que ci-dessus (getUserRoles → isAdminFederal check)
- Dans `actions.default` : même check avant l'INSERT (permission vérifiée côté serveur même si on vient du form)
- Conserver l'INSERT SQL existant intact

NOTE sur la logique admin : L'admin fédéral a son rôle scopé sur l'entité fédération dans user_entity_role. `getUserRoles` retourne tous ses rôles. Vérifier `.some(r => r.role === "adminFederal")` est correct — peu importe l'entité, si quelqu'un est adminFederal quelque part, il peut gérer toutes les entités (selon le modèle décidé).
  </action>
  <verify>
    <automated>cd /home/jta/Projects/darts-management/master && grep -l "getUserRoles\|checkRole" "packages/front/src/routes/(app)/admin/+page.server.ts" "packages/front/src/routes/(app)/admin/entities/new/+page.server.ts" && ! grep -q "userHasPermission" "packages/front/src/routes/(app)/admin/+page.server.ts" && ! grep -q "userHasPermission" "packages/front/src/routes/(app)/admin/entities/new/+page.server.ts" && echo "GUARDS OK"</automated>
  </verify>
  <done>Les deux fichiers utilisent getUserRoles au lieu de userHasPermission. pnpm typecheck passe sur ces fichiers.</done>
</task>

<task type="auto">
  <name>Task 2: Mettre à jour layout server + navbar pour affichage conditionnel Admin</name>
  <files>
    packages/front/src/routes/(app)/+layout.server.ts
    packages/front/src/routes/(app)/+layout.svelte
    packages/front/src/lib/auth-client.ts
  </files>
  <action>
**Lire d'abord les trois fichiers existants** pour comprendre leur structure actuelle.

**+layout.server.ts** — transmettre les rôles au layout :
Actuellement il retourne `{ user, session }`. Ajouter les rôles :
```typescript
import { getUserRoles } from "@darts-management/db"

export const load: LayoutServerLoad = async ({ locals }) => {
  if (!locals.user) redirect(302, "/login")
  const roles = await getUserRoles(locals.user.id)
  const hasAdminAccess = roles.some(r =>
    ["adminFederal", "adminLigue", "adminComite", "adminClub"].includes(r.role)
  )
  return {
    user: locals.user,
    session: locals.session,
    hasAdminAccess,
  }
}
```

**+layout.svelte** — corriger l'affichage conditionnel du lien Administration :
Actuellement la navbar conditionne le lien Admin sur le rôle Better Auth (ex: `user?.role === 'adminFederal'` ou via authClient). Remplacer par `data.hasAdminAccess` reçu du layout server.

Chercher dans le fichier le bloc conditionnel qui affiche "Administration" et remplacer la condition par `{#if data.hasAdminAccess}`.

**auth-client.ts** — retirer adminClient() si plus utilisé :
```typescript
// AVANT
import { adminClient } from "better-auth/client/plugins"
export const authClient = createAuthClient({ plugins: [adminClient()] })

// APRÈS (le plugin admin est retiré côté serveur, inutile côté client)
import { createAuthClient } from "better-auth/svelte"
export const authClient = createAuthClient()
```

NOTE : Si d'autres composants utilisent `authClient.admin.*` ou `authClient.$Infer.*` liés au plugin admin, les supprimer aussi. Rechercher `authClient.admin` dans packages/front/src/ avant de retirer adminClient.
  </action>
  <verify>
    <automated>cd /home/jta/Projects/darts-management/master && pnpm --filter @darts-management/front typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>pnpm typecheck passe sur packages/front. La navbar utilise data.hasAdminAccess. auth-client.ts n'importe plus adminClient. Le layout server transmet hasAdminAccess.</done>
</task>

</tasks>

<verification>
Après les 2 tâches :

1. `grep -r "userHasPermission" packages/front/src/` — doit retourner 0 résultats
2. `grep -r "adminClient\|adminPlugin" packages/front/src/` — doit retourner 0 résultats
3. `pnpm --filter @darts-management/front typecheck` — doit passer sans erreur
4. `grep "hasAdminAccess" packages/front/src/routes/\(app\)/+layout.server.ts` — doit trouver le champ
5. `grep "hasAdminAccess" packages/front/src/routes/\(app\)/+layout.svelte` — doit trouver la condition
</verification>

<success_criteria>
- Aucun appel à auth.api.userHasPermission dans le frontend
- Aucun import de adminClient dans packages/front
- Les gardes admin utilisent getUserRoles() depuis @darts-management/db
- La navbar affiche "Administration" basé sur data.hasAdminAccess (serveur)
- pnpm typecheck passe sur packages/front et packages/db
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md`
</output>
