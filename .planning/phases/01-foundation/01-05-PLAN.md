---
phase: 01-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/004_user_entity_role.sql
  - packages/db/src/schema/005_drop_role_column.sql
  - packages/db/src/schema/003_seed_dev.sql
  - packages/db/src/authz.ts
  - packages/db/src/auth.ts
  - packages/db/src/index.ts
autonomous: true
gap_closure: true
requirements:
  - AUTH-04
  - ORG-01
  - ORG-02
  - ORG-03

must_haves:
  truths:
    - "La table user_entity_role existe avec colonnes user_id, entity_id, role et contrainte PK composite"
    - "La colonne role est supprimée de la table user (Better Auth admin plugin retiré)"
    - "packages/db exporte une fonction checkRole(userId, entityId, role) utilisable par packages/front"
    - "Le seed dev insère des enregistrements dans user_entity_role pour les 4 utilisateurs de test"
    - "auth.ts n'importe plus adminPlugin ni createAccessControl"
  artifacts:
    - path: "packages/db/src/schema/004_user_entity_role.sql"
      provides: "Migration CREATE TABLE user_entity_role"
      contains: "user_entity_role"
    - path: "packages/db/src/schema/005_drop_role_column.sql"
      provides: "Migration DROP COLUMN role on user table"
      contains: "DROP COLUMN"
    - path: "packages/db/src/authz.ts"
      provides: "Custom authorization layer — checkRole, getUserRoles, canPromote"
      exports: ["checkRole", "getUserRoles", "canPromote"]
    - path: "packages/db/src/auth.ts"
      provides: "Better Auth sans admin plugin"
  key_links:
    - from: "packages/db/src/authz.ts"
      to: "packages/db/src/client.ts"
      via: "sql tagged template queries"
      pattern: "sql`SELECT.*user_entity_role"
    - from: "packages/db/src/index.ts"
      to: "packages/db/src/authz.ts"
      via: "export { checkRole, getUserRoles, canPromote }"
      pattern: "export.*authz"
---

<objective>
Remplacer le système de rôles Better Auth (plugin admin + colonne `role` sur `user`) par une table `user_entity_role(user_id, entity_id, role)` et une couche d'autorisation custom.

Purpose: Le plugin admin Better Auth est conçu pour des rôles globaux simples. La hiérarchie fédérale nécessite des droits scopés par entité avec des règles de promotion hiérarchiques — cela ne peut pas être modélisé dans une colonne `role` globale.
Output: Deux migrations SQL, un module `authz.ts` exportant `checkRole/getUserRoles/canPromote`, `auth.ts` sans admin plugin, `index.ts` mis à jour, seed mis à jour.
</objective>

<execution_context>
@/home/jta/.claude/get-shit-done/workflows/execute-plan.md
@/home/jta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Modèle d'autorisation (from gap_decisions in CONTEXT.md) -->
<!--
Rôles valides dans user_entity_role :
  'organisateur' | 'adminTournoi' | 'adminClub' | 'adminComite' | 'adminLigue' | 'adminFederal'

Règle : joueur = implicite pour tout user authentifié, NON stocké dans user_entity_role.

Règle de promotion :
  Un admin peut assigner un rôle <= son propre niveau, uniquement sur les entités
  où il détient ce rôle.

Hiérarchie de rôles (du plus élevé au plus bas) :
  adminFederal > adminLigue > adminComite > adminClub > adminTournoi > organisateur

Pas d'héritage implicite : chaque droit est explicite sur son entité.
-->

<!-- Contrainte : les colonnes Better Auth sont en camelCase (bug connu #4789) -->
<!-- La table user a une colonne "role" TEXT ajoutée par le plugin admin -->
<!-- Après migration 005, cette colonne est supprimée -->

From packages/db/src/client.ts (existing):
```typescript
import postgres from "postgres"
export const sql = postgres(process.env["DATABASE_URL"] ?? "")
```

From packages/db/src/index.ts (current):
```typescript
export { sql } from "./client.js"
export { auth } from "./auth.js"
// Après ce plan : ajouter export de authz
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations SQL — user_entity_role et suppression role</name>
  <files>
    packages/db/src/schema/004_user_entity_role.sql
    packages/db/src/schema/005_drop_role_column.sql
  </files>
  <action>
Créer deux fichiers de migration SQL (node-pg-migrate les exécute en ordre alphabétique/numérique).

**004_user_entity_role.sql** :
```sql
-- Migration: Create user_entity_role table
-- Remplace la colonne role globale par des rôles scopés par entité

CREATE TYPE entity_role AS ENUM (
  'organisateur',
  'adminTournoi',
  'adminClub',
  'adminComite',
  'adminLigue',
  'adminFederal'
);

CREATE TABLE user_entity_role (
  user_id    TEXT NOT NULL,
  entity_id  UUID NOT NULL REFERENCES entity(id) ON DELETE CASCADE,
  role       entity_role NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),

  PRIMARY KEY (user_id, entity_id, role)
);

CREATE INDEX user_entity_role_user_idx ON user_entity_role(user_id);
CREATE INDEX user_entity_role_entity_idx ON user_entity_role(entity_id);
```

NOTE : user_id est TEXT (pas UUID) — Better Auth stocke les IDs user en TEXT (camelCase schema).

**005_drop_role_column.sql** :
```sql
-- Migration: Remove Better Auth admin plugin role column
-- Le plugin admin n'est plus utilisé ; la colonne role est remplacée par user_entity_role

ALTER TABLE "user" DROP COLUMN IF EXISTS role;
```

NOTE : La table Better Auth s'appelle "user" (entre guillemets car mot réservé SQL).
  </action>
  <verify>
    <automated>cd /home/jta/Projects/darts-management/master && grep -l "user_entity_role" packages/db/src/schema/004_user_entity_role.sql && grep -l "DROP COLUMN" packages/db/src/schema/005_drop_role_column.sql && echo "MIGRATIONS OK"</automated>
  </verify>
  <done>Les deux fichiers de migration existent avec le contenu correct. La table user_entity_role a une PK composite (user_id, entity_id, role), un ENUM entity_role, et des index. La migration 005 supprime la colonne role.</done>
</task>

<task type="auto">
  <name>Task 2: Module authz.ts — couche d'autorisation custom</name>
  <files>
    packages/db/src/authz.ts
    packages/db/src/index.ts
  </files>
  <action>
Créer `packages/db/src/authz.ts` avec trois exports :

```typescript
import { sql } from "./client.js"

export type EntityRole =
  | "organisateur"
  | "adminTournoi"
  | "adminClub"
  | "adminComite"
  | "adminLigue"
  | "adminFederal"

// Hiérarchie pour les règles de promotion (index = niveau, plus haut = plus de droits)
const ROLE_HIERARCHY: EntityRole[] = [
  "organisateur",
  "adminTournoi",
  "adminClub",
  "adminComite",
  "adminLigue",
  "adminFederal",
]

/**
 * Vérifie si un utilisateur a un rôle spécifique sur une entité donnée.
 * Note : le rôle "joueur" est implicite pour tout utilisateur authentifié — ne pas appeler
 * cette fonction pour vérifier joueur.
 */
export async function checkRole(
  userId: string,
  entityId: string,
  role: EntityRole,
): Promise<boolean> {
  const rows = await sql<{ count: string }[]>`
    SELECT COUNT(*) as count
    FROM user_entity_role
    WHERE user_id = ${userId}
      AND entity_id = ${entityId}
      AND role = ${role}
  `
  return Number(rows[0]?.count ?? 0) > 0
}

/**
 * Retourne tous les rôles d'un utilisateur (sur toutes les entités).
 */
export async function getUserRoles(
  userId: string,
): Promise<Array<{ entityId: string; role: EntityRole }>> {
  const rows = await sql<Array<{ entity_id: string; role: EntityRole }>>`
    SELECT entity_id, role
    FROM user_entity_role
    WHERE user_id = ${userId}
  `
  return rows.map((r) => ({ entityId: r.entity_id, role: r.role }))
}

/**
 * Vérifie si promoter peut assigner role à un autre user sur entityId.
 * Règle : un admin peut promouvoir jusqu'à son propre niveau, uniquement sur les entités
 * où il a ce niveau ou supérieur.
 */
export async function canPromote(
  promoterId: string,
  entityId: string,
  targetRole: EntityRole,
): Promise<boolean> {
  const promoterRoles = await getUserRoles(promoterId)
  const promoterEntityRoles = promoterRoles
    .filter((r) => r.entityId === entityId)
    .map((r) => r.role)

  const promoterMaxLevel = Math.max(
    -1,
    ...promoterEntityRoles.map((r) => ROLE_HIERARCHY.indexOf(r)),
  )
  const targetLevel = ROLE_HIERARCHY.indexOf(targetRole)

  // adminFederal peut promouvoir sur n'importe quelle entité
  const isAdminFederal = promoterRoles.some((r) => r.role === "adminFederal")
  if (isAdminFederal) return targetLevel <= ROLE_HIERARCHY.indexOf("adminFederal")

  return promoterMaxLevel >= 0 && targetLevel <= promoterMaxLevel
}
```

Ensuite mettre à jour `packages/db/src/index.ts` pour exporter le module authz :
```typescript
export { sql } from "./client.js"
export { auth } from "./auth.js"
export { checkRole, getUserRoles, canPromote } from "./authz.js"
export type { EntityRole } from "./authz.js"
```
  </action>
  <verify>
    <automated>cd /home/jta/Projects/darts-management/master && pnpm --filter @darts-management/db typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>packages/db passe le typecheck sans erreur. authz.ts exporte checkRole, getUserRoles, canPromote. index.ts les ré-exporte. Les types EntityRole sont exportés.</done>
</task>

<task type="auto">
  <name>Task 3: Retirer admin plugin de auth.ts + mettre à jour seed</name>
  <files>
    packages/db/src/auth.ts
    packages/db/src/schema/003_seed_dev.sql
  </files>
  <action>
**Modifier packages/db/src/auth.ts** :
- Supprimer l'import de `admin as adminPlugin` depuis `better-auth/plugins`
- Supprimer l'import de `ac, joueur, organisateur, adminTournoi, adminFederal` depuis `./permissions.js`
- Retirer le plugin `adminPlugin({...})` du tableau `plugins`
- Conserver uniquement `sveltekitCookies(getRequestEvent)` dans `plugins`
- Conserver `emailAndPassword` tel quel
- Ne PAS supprimer le fichier permissions.ts maintenant (frontend le référence peut-être encore — nettoyage en plan 06)

NOTE : après la migration 005, la colonne `role` n'existe plus sur `user`. Le plugin admin ne peut plus être utilisé de toute façon.

**Modifier packages/db/src/schema/003_seed_dev.sql** :
Ajouter des INSERT dans user_entity_role après les INSERT existants.
Les IDs des entités et users dans le seed doivent correspondre aux UUIDs déjà définis dans 003_seed_dev.sql.

Lire le fichier 003_seed_dev.sql existant pour récupérer les UUIDs exacts, puis ajouter à la fin :

```sql
-- Rôles des utilisateurs de test
-- federal@test.ffd.fr → adminFederal sur la fédération
INSERT INTO user_entity_role (user_id, entity_id, role) VALUES
  ('<user_id_federal>', '<entity_id_federation>', 'adminFederal');

-- organisateur@test.ffd.fr → organisateur sur Ligue Île-de-France
INSERT INTO user_entity_role (user_id, entity_id, role) VALUES
  ('<user_id_orga>', '<entity_id_ligue_idf>', 'organisateur');

-- admin@test.ffd.fr (adminTournoi) → adminTournoi sur un comité
INSERT INTO user_entity_role (user_id, entity_id, role) VALUES
  ('<user_id_admin>', '<entity_id_comite>', 'adminTournoi');

-- joueur@test.ffd.fr → aucun rôle dans user_entity_role (joueur est implicite)
```

IMPORTANT : remplacer les placeholders `<...>` par les UUIDs réels lus dans 003_seed_dev.sql.
  </action>
  <verify>
    <automated>cd /home/jta/Projects/darts-management/master && grep -c "adminPlugin" packages/db/src/auth.ts || echo "0" && pnpm --filter @darts-management/db typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>auth.ts ne contient plus d'import ou d'usage de adminPlugin. Le typecheck passe. 003_seed_dev.sql contient des INSERT dans user_entity_role avec des UUIDs réels (pas de placeholders).</done>
</task>

</tasks>

<verification>
Après les 3 tâches :

1. `grep -r "adminPlugin" packages/db/src/` — doit retourner 0 résultats
2. `grep "user_entity_role" packages/db/src/schema/004_user_entity_role.sql` — doit trouver le CREATE TABLE
3. `pnpm --filter @darts-management/db typecheck` — doit passer sans erreur
4. `grep "checkRole\|getUserRoles\|canPromote" packages/db/src/index.ts` — doit trouver les 3 exports
</verification>

<success_criteria>
- Table user_entity_role définie en SQL avec ENUM entity_role et index appropriés
- La colonne role est supprimée de la table user dans la migration 005
- packages/db/src/authz.ts exporte checkRole, getUserRoles, canPromote avec types corrects
- packages/db/src/auth.ts n'utilise plus le plugin admin Better Auth
- Le seed insère des rôles réels dans user_entity_role pour les users de test non-joueurs
- pnpm typecheck passe sur packages/db
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
