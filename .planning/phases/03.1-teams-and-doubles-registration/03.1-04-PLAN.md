---
phase: 03.1-teams-and-doubles-registration
plan: "04"
type: execute
wave: 3
depends_on:
  - "03.1-02"
  - "03.1-03"
files_modified:
  - packages/front/src/lib/tournament/components/DoublesModal.svelte
  - packages/front/src/lib/tournament/components/PlayerSearch.svelte
  - packages/front/src/routes/(app)/events/[id]/+page.svelte
  - packages/front/src/routes/tournaments/[id]/admin/+page.svelte
  - packages/front/src/routes/tournaments/[id]/admin/unregister/+server.ts
autonomous: false
requirements:
  - TEAM-03
  - TEAM-04

must_haves:
  truths:
    - "Un joueur cliquant S'inscrire sur un tournoi indiv est inscrit directement (solo team auto-créée)"
    - "Un joueur cliquant S'inscrire sur un tournoi doubles voit la modal DoublesModal"
    - "La modal doubles permet de rechercher un partenaire à partir de 3 caractères avec résultats en temps réel"
    - "La modal doubles permet de créer un nouveau joueur (prénom, nom, département) comme partenaire"
    - "La confirmation dans la modal déclenche POST /events/[id]/register avec partner_player_id ou new_partner"
    - "Le roster admin affiche les membres de chaque équipe (1 ou 2 joueurs selon la catégorie)"
    - "Le bouton Retirer dans le roster admin passe team_id (plus player_id)"
    - "PlayerSearch affiche le département des joueurs dans les résultats"
    - "L'admin peut inscrire un joueur via PlayerSearch — team solo créée côté serveur"
  artifacts:
    - path: "packages/front/src/lib/tournament/components/DoublesModal.svelte"
      provides: "Modal de sélection de partenaire pour inscription doubles"
      min_lines: 80
    - path: "packages/front/src/routes/(app)/events/[id]/+page.svelte"
      provides: "Page publique avec flux indiv/doubles branché sur la catégorie"
      contains: "isDoubles"
    - path: "packages/front/src/routes/tournaments/[id]/admin/+page.svelte"
      provides: "Roster admin avec members[] et unregister par team_id"
      contains: "team_id"
    - path: "packages/front/src/routes/tournaments/[id]/admin/unregister/+server.ts"
      provides: "Endpoint unregister refactorisé vers team_id"
      contains: "team_id"
  key_links:
    - from: "events/[id]/+page.svelte bouton doubles"
      to: "DoublesModal.svelte"
      via: "openDoublesModal(tournament) → DoublesModal open prop"
      pattern: "openDoublesModal"
    - from: "DoublesModal.svelte confirm()"
      to: "/events/[id]/register POST"
      via: "fetch avec partner_player_id ou new_partner"
      pattern: "partner_player_id"
    - from: "admin/+page.svelte unregister()"
      to: "/tournaments/[id]/admin/unregister DELETE"
      via: "JSON body { team_id }"
      pattern: "team_id"
---

<objective>
Créer DoublesModal.svelte et mettre à jour les composants et pages UI pour brancher le flux doubles (modal) et le flux indiv (inscription directe), afficher les membres d'équipe dans le roster admin, et corriger le endpoint unregister admin vers team_id.

Purpose: Ce plan livre la couche UI visible par l'utilisateur — la modal doubles est la fonctionnalité clé de Phase 03.1.
Output: DoublesModal + pages événement et admin roster refactorisées.
</objective>

<execution_context>
@/home/jta/.claude/get-shit-done/workflows/execute-plan.md
@/home/jta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03.1-teams-and-doubles-registration/03.1-02-SUMMARY.md
@.planning/phases/03.1-teams-and-doubles-registration/03.1-03-SUMMARY.md
@packages/front/src/routes/(app)/events/[id]/+page.svelte
@packages/front/src/routes/tournaments/[id]/admin/+page.svelte
@packages/front/src/routes/tournaments/[id]/admin/unregister/+server.ts
@packages/front/src/lib/tournament/components/PlayerSearch.svelte

<interfaces>
<!-- Schemas disponibles depuis Plan 01 -->
```typescript
// PartnerSearchResult — retourné par GET /tournaments/[id]/partner/search
export type PartnerSearchResult = z.infer<typeof PartnerSearchResultSchema>
// { id: string, first_name: string, last_name: string, department: string | null }

// RosterEntry — retourné par admin/+page.server.ts (members[])
export type RosterEntry = z.infer<typeof RosterEntrySchema>
// { registration_id: string, team_id: string, members: Array<{ player_id, first_name, last_name, department }>, checked_in: boolean, registered_at: Date }

// Category — inclut les variantes doubles
export type Category = "male" | "female" | "junior" | "veteran" | "open" | "mix" | "double" | "double_female" | "double_mix"
```

<!-- Pattern Svelte 5 runes — décisions passées -->
```svelte
<!-- Props Svelte 5 : let { prop } = $props() -->
<!-- État réactif : let x = $state(initialValue) -->
<!-- Effets : $effect(() => { ... }) -->
<!-- Bindable : let { open = $bindable() } = $props() -->
```

<!-- Endpoint register self-service refactorisé (Plan 02) -->
```typescript
// POST /events/[id]/register accepte :
// { tournament_id, partner_player_id? } — doubles avec partenaire existant
// { tournament_id, new_partner: { first_name, last_name, department } } — doubles avec nouveau joueur
// { tournament_id } — solo indiv

// DELETE /events/[id]/register accepte :
// { tournament_id } — supprime via team_member JOIN (idem qu'avant pour le corps)
```

<!-- Unregister admin ACTUEL — à refactoriser -->
```typescript
// AVANT : UnregisterSchema = z.object({ player_id: z.string().uuid() })
// DELETE WHERE tournament_id = $1 AND player_id = $2
// APRÈS : UnregisterSchema = z.object({ team_id: z.string().uuid() })
// DELETE WHERE tournament_id = $1 AND team_id = $2
```

<!-- Pattern debounce déjà utilisé dans PlayerSearch.svelte -->
```typescript
let timer: ReturnType<typeof setTimeout>
$effect(() => {
  clearTimeout(timer)
  if (query.length < 3) { results = []; return }
  timer = setTimeout(async () => { /* fetch */ }, 300)
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer DoublesModal.svelte + mettre à jour events/[id]/+page.svelte</name>
  <files>
    packages/front/src/lib/tournament/components/DoublesModal.svelte
    packages/front/src/routes/(app)/events/[id]/+page.svelte
  </files>
  <action>
**Fichier 1 : packages/front/src/lib/tournament/components/DoublesModal.svelte**

Nouveau composant Svelte 5. Utiliser Modal de flowbite-svelte.

Props :
```typescript
type Props = {
  open: boolean
  tournamentId: string
  eventId: string
  onClose: () => void
  onRegistered: () => void
}
let { open = $bindable(), tournamentId, eventId, onClose, onRegistered }: Props = $props()
```

État interne :
```typescript
let query = $state('')
let results = $state<PartnerSearchResult[]>([])
let selected = $state<PartnerSearchResult | null>(null)
let showCreateForm = $state(false)
let newPartner = $state({ first_name: '', last_name: '', department: '' })
let submitting = $state(false)
let error = $state<string | null>(null)
let timer: ReturnType<typeof setTimeout>
```

Effet debounce (300ms, seuil 3 chars) pour appeler GET /tournaments/{tournamentId}/partner/search?q={query}.

Fonctions :
- `selectPartner(p)` : selected = p, vider query et results, showCreateForm = false
- `confirm()` : construire body selon selected ou showCreateForm, POST /events/{eventId}/register, si ok → onRegistered() + open = false, sinon → error = message
- `reset()` : vider tous les états locaux

Affichage :
1. Input de recherche (bind:value={query})
2. Liste des résultats si results.length > 0 — chaque item affiche "{last_name} {first_name}{department ? ` (${department})` : ''}" — cliquable via selectPartner(p)
3. Si selected : afficher "Partenaire sélectionné : {selected.last_name} {selected.first_name}" + bouton Changer
4. Bouton "Joueur non trouvé ? Créer un nouveau joueur" — toggle showCreateForm, selected = null
5. Si showCreateForm : 3 inputs (Prénom, Nom, Département — ex: "31 - Haute-Garonne") en grille 2 cols
6. Message d'erreur si error
7. Footer : bouton Confirmer (disabled si !selected && !showCreateForm, ou si submitting) + bouton Annuler

Appeler `svelte-autofixer` (MCP) sur ce composant avant de le considérer terminé — corriger tous les problèmes signalés notamment la syntaxe Modal footer Flowbite-Svelte en Svelte 5.

---

**Fichier 2 : packages/front/src/routes/(app)/events/[id]/+page.svelte**

Changements :
1. Importer DoublesModal depuis `$lib/tournament/components/DoublesModal.svelte`
2. Ajouter le helper :
```typescript
const DOUBLES_CATEGORIES = ['double', 'double_female', 'double_mix'] as const
function isDoubles(category: string): boolean {
  return (DOUBLES_CATEGORIES as readonly string[]).includes(category)
}
```
3. Ajouter l'état pour la modal :
```typescript
let doublesModal = $state<{ open: boolean; tournamentId: string }>({ open: false, tournamentId: '' })

function openDoublesModal(tournamentId: string) {
  doublesModal = { open: true, tournamentId }
}
```
4. Renommer la fonction `register` en `registerSolo` — elle passe `{ tournament_id }` sans partner (solo indiv).
5. Dans la boucle des tournois, brancher sur isDoubles :
```svelte
{#if tournament.is_registered}
  <Button color="red" size="xs" onclick={() => unregister(tournament.id)}>Se désinscrire</Button>
{:else if isDoubles(tournament.category)}
  <Button color="primary" size="xs" onclick={() => openDoublesModal(tournament.id)}>
    S'inscrire (doubles)
  </Button>
{:else}
  <Button color="primary" size="xs" onclick={() => registerSolo(tournament.id)}>
    S'inscrire
  </Button>
{/if}
```
6. Ajouter la modal après le contenu principal :
```svelte
<DoublesModal
  bind:open={doublesModal.open}
  tournamentId={doublesModal.tournamentId}
  eventId={data.event.id}
  onClose={() => doublesModal.open = false}
  onRegistered={() => {
    const t = tournaments.find(t => t.id === doublesModal.tournamentId)
    if (t) { t.is_registered = true; t.registration_count++ }
    doublesModal.open = false
  }}
/>
```

Appeler `svelte-autofixer` (MCP) sur +page.svelte après modification.
  </action>
  <verify>
    <automated>cd /home/jta/Projects/darts-management/master && pnpm typecheck 2>&1 | grep -E "DoublesModal|page\.svelte|error TS" | head -20</automated>
  </verify>
  <done>DoublesModal.svelte créé sans erreurs TypeScript/Svelte. +page.svelte a les deux branches (isDoubles → modal, sinon → registerSolo). pnpm typecheck vert sur ces fichiers.</done>
</task>

<task type="auto">
  <name>Task 2: Refactorer admin roster UI + unregister endpoint + PlayerSearch département</name>
  <files>
    packages/front/src/routes/tournaments/[id]/admin/unregister/+server.ts
    packages/front/src/routes/tournaments/[id]/admin/+page.svelte
    packages/front/src/lib/tournament/components/PlayerSearch.svelte
  </files>
  <action>
**Fichier 1 : packages/front/src/routes/tournaments/[id]/admin/unregister/+server.ts**

Remplacer `UnregisterSchema` :
```typescript
// AVANT
const UnregisterSchema = z.object({ player_id: z.string().uuid() })
// APRÈS
const UnregisterSchema = z.object({ team_id: z.string().uuid() })
```

Mettre à jour la DELETE query :
```sql
-- AVANT
DELETE FROM tournament_registration
WHERE tournament_id = ${params.id} AND player_id = ${body.player_id}
-- APRÈS
DELETE FROM tournament_registration
WHERE tournament_id = ${params.id} AND team_id = ${body.team_id}
```

---

**Fichier 2 : packages/front/src/routes/tournaments/[id]/admin/+page.svelte**

Changements requis :

**a) Fonction `unregister` — passer team_id au lieu de player_id :**
```typescript
async function unregister(teamId: string) {
  await fetch(`/tournaments/${data.tournament.id}/admin/unregister`, {
    method: "DELETE",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ team_id: teamId })
  })
  roster = roster.filter((e) => e.team_id !== teamId)
}
```

**b) Tableau roster — adapter pour members[] :**

Remplacer l'affichage de la ligne joueur :
- `entry.last_name entry.first_name` → Itérer sur `entry.members` pour afficher chaque membre (NOM Prénom, département si présent)
- `entry.licence_no` → Supprimer cette colonne (absente de RosterEntry après refactor)
- Bouton Retirer → `onclick={() => unregister(entry.team_id)}`

Pour une équipe solo (1 membre) : afficher 1 ligne. Pour doubles (2 membres) : afficher les 2 joueurs séparés par un " / " ou sur deux lignes dans la même cellule.

Exemple d'affichage membres :
```svelte
<TableBodyCell>
  {#each entry.members as member}
    <div>{member.last_name} {member.first_name}{member.department ? ` (${member.department})` : ''}</div>
  {/each}
</TableBodyCell>
```

**c) Compteur header :**
Le texte `{roster.length} joueur(s) inscrit(s)` peut devenir `{roster.length} équipe(s) inscrite(s)` — équipe = 1 joueur pour indiv, 2 pour doubles.

**d) Formulaire "nouveau joueur" — ajouter champ département :**
Ajouter `let newDepartment = $state('')` et un Input département dans le formulaire `showNewPlayerForm`. Passer `department: newDepartment || undefined` dans le body de registerNew.

**e) Affichage selectedPlayer — ajouter département :**
Ajouter `{#if selectedPlayer.department}<span class="text-sm text-gray-500">({selectedPlayer.department})</span>{/if}` après le nom.

---

**Fichier 3 : packages/front/src/lib/tournament/components/PlayerSearch.svelte**

Lire le fichier en entier d'abord (utiliser le Read tool).
Ajouter l'affichage du département dans les résultats de recherche :
```svelte
<!-- Après le bloc nom (last_name first_name) -->
{#if player.department}
  <span class="text-gray-400 dark:text-gray-500 text-xs">({player.department})</span>
{/if}
```

PlayerSearchResult a maintenant `department: string | null` — le type est déjà mis à jour dans event-schemas.ts (Plan 01).

Appeler `svelte-autofixer` (MCP) sur admin/+page.svelte et PlayerSearch.svelte après modification.
  </action>
  <verify>
    <automated>cd /home/jta/Projects/darts-management/master && pnpm typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>pnpm typecheck vert. Le bouton Retirer dans le roster admin passe team_id. L'endpoint unregister supprime par team_id. PlayerSearch affiche le département. Le formulaire nouveau joueur admin inclut le champ département.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Vérification humaine — Phase 03.1 complète</name>
  <action>Vérification manuelle de l'ensemble du flux équipes et inscription doubles</action>
  <verify>Voir how-to-verify ci-dessous</verify>
  <done>Checkpoint humain approuvé : inscription indiv directe, modal doubles fonctionnelle, roster admin team-based</done>
  <what-built>
Phase 03.1 complète : modèle équipes en DB, endpoints refactorisés, modal doubles, roster admin mis à jour.
  </what-built>
  <how-to-verify>
1. Démarrer l'application : `pnpm dev`
2. Créer un compte joueur A — vérifier que le profil joueur est créé (pas d'erreur sur department nullable)
3. Ouvrir un événement avec au moins un tournoi indiv (ex: "male") — cliquer S'inscrire — vérifier inscription directe
4. Ouvrir un événement avec un tournoi doubles — cliquer S'inscrire (doubles) — vérifier que la modal s'ouvre
5. Dans la modal : rechercher un partenaire (taper au moins 3 caractères) — vérifier que les résultats apparaissent avec département
6. Sélectionner un partenaire — Confirmer — vérifier inscription réussie (is_registered = true)
7. Tester "Créer un nouveau joueur" dans la modal : saisir prénom, nom, département — Confirmer
8. Ouvrir /tournaments/[id]/admin — vérifier que le roster affiche les membres d'équipe (1 joueur pour indiv, 2 pour doubles)
9. Cliquer Retirer sur une inscription — vérifier que la ligne disparaît
10. Tester la recherche joueur admin : vérifier que le département est affiché dans les résultats
  </how-to-verify>
  <resume-signal>Taper "approved" si tout fonctionne, ou décrire les problèmes observés</resume-signal>
</task>

</tasks>

<verification>
1. pnpm typecheck vert sur l'ensemble du projet.
2. Un joueur peut s'inscrire à un tournoi indiv (inscription directe, team solo créée).
3. Un joueur peut s'inscrire à un tournoi doubles via la modal (partenaire existant ou nouveau joueur).
4. Le roster admin affiche les membres d'équipe avec département.
5. Retirer une inscription depuis le roster admin fonctionne via team_id.
</verification>

<success_criteria>
- DoublesModal.svelte créé et fonctionnel (recherche debounce, sélection partenaire, création nouveau joueur, confirmation).
- /events/[id]/+page.svelte branche sur isDoubles(category) pour ouvrir la modal ou inscrire directement.
- /tournaments/[id]/admin/+page.svelte affiche entry.members[] (NOM Prénom département).
- admin/unregister endpoint supprime par team_id.
- PlayerSearch affiche le département.
- pnpm typecheck vert.
- Checkpoint humain approuvé.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-teams-and-doubles-registration/03.1-04-SUMMARY.md`
</output>
